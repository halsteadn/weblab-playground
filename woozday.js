/*  Stunning Post-Flood Mud Crust 
The best writing is rewriting ~ E.B.White
First do it, then do it right, then do it better ~ Addy Osmani*/



navigator.geolocation.getCurrentPosition(function(details) {
  console.log(JSON.stringify(details));
});

if(navigator.userAgent.match(/i(Pod|Pad|Phone)/i)) {
  alert('Seems to be iOS');
}
return navigator.userAgent;

var myArray = [ 1, 2, 3 ];
for(var index = 0; index < myArray.length; index++) {
    console.log("old school: " + myArray[]);
    results.push(myArray[index] + 3);
}
console.log("hard: " + results);

var easy = _.map(myArray, function(item)    {
    return item = 3
});
easy = _.map (myArray, function(item)   {
console.log("easy: " + results);
}
              
      /*
      * EVENT HANDLER SLIDES 
      */
              
              var onkeyup = function(ev) {
  console.log('keyup: ' + ev.which);
}

var onclickButton = function()  {
    alert('User name: ' + $('.user-name').val());
}
$(user-name)
$('.show-user').click(function() {
})


// $(this) is shorthand for $(ev.target) //
$('ul.list li').click(function(ev) {
  var target = $(ev.target);
  console.log('Clicked: ' + target.text() + ' at ' + ev.pageX + ', ' + ev.pageY);
});
// $(this) is shorthand for $(ev.target) //
ev.stopPropagation()
//keeps an event from "Bubbling Up… jQuery calls it "event bubbling" or "event propagation"
